export const WalletCoinList = ({ assetsMap }) => {
	console.log(assetsMap);
	return (
		<div className="flex flex-col">
			{/* {assetsMap &&
				assetsMap.map((asset) => {
					return <p>{`${asset[Object.entries(asset)]} - ${asset}`}</p>;
				})} */}
		</div>
	);
};
